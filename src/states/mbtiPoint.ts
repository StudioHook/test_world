import { atom } from 'recoil';

export const EIPoint = atom({
  key: 'EIPoint',
  default: 0,
});

export const SNPoint = atom({
  key: 'SNPoint',
  default: 0,
});

export const TFPoint = atom({
  key: 'TFPoint',
  default: 0,
});

export const JPPoint = atom({
  key: 'JPPoint',
  default: 0,
});
